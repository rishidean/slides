# Draft snapshots — 11 Aug 2026

Working copies of the Airborne and Atmosphere decks from the day they were rewritten, kept for
history. **None of these are live.** The served decks are `airborne/index.html` and
`atmosphere/index.html`.

The rewrite happened in an ephemeral container that no longer exists, so these files were the only
record of the work until it landed in commit `78538ac`.

| File | What it is |
|---|---|
| `downloads-airborne-index.html` | Airborne, first delivery (11:42). Superseded. |
| `downloads-airborne-index_1.html` | Airborne, revised delivery (13:12). **This is what shipped.** Smaller than the 11:42 file because Part 1 was cut down to a keynote recap. |
| `downloads-atmosphere-breakingatmosphere.html` | Atmosphere, first delivery (11:42). Superseded. |
| `downloads-atmosphere-breakingatmosphere_1.html` | Atmosphere, revised delivery (13:12). **This is what shipped** — verified byte-identical to `atmosphere/index.html` once the vendored-font `<link>` swap is applied. |
| `repo-atmosphere-breaking-atmosphere-jul11.html` | Intermediate Atmosphere draft from 11 Jul, found untracked in `atmosphere/`. Has the Spaceship rename but not the Independent-convergence slide. An identical copy was also sitting on the Desktop; only one is kept here. |
| `repo-atmosphere-REGRESSED-backup-jul10.html` | A 10 Jul backup labelled REGRESSED by whoever made it. Kept only because it was in the deck directory. |

These are static HTML and GitHub Pages serves this directory, so they are publicly reachable —
fine, since the decks themselves are public, but don't put anything private here.
