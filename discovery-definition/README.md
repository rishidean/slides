# Discovery & Definition (Workshop 1)

Agentic Engineering Workshop, Parts 2–3 of 5. One 90-minute hands-on session: Discovery develops an idea with an LLM into resolved product decisions; Definition synthesizes those decisions into the artifacts-only PEDAL `defined` repository.

Source of truth for content: `escape-velocity/DISCOVERY/Escape Velocity: DISCOVERY + DEFINITION Workshop Architecture.md`. The demo spine and exercise references live in the PEDAL discovery packet (`escape-velocity/PEDAL/facilitator/discovery/`) and the `defined` repo state (`escape-velocity/PEDAL/defined/`).

Static deck. `index.html` loads `deck-stage.js`, `support.js`, `image-slot.js`, and `uploads/` by relative path — keep them side by side. `.nojekyll` stops GitHub Pages from running Jekyll over the files. Speaker notes: N key; presentation mode: P key.
