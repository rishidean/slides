# Getting Airborne

Workshop deck — Demystifying agentic coding with Claude Code.

Live: https://slides.rishidean.com/airborne/

Static deck. `index.html` loads `deck-stage.js`, `support.js`, `image-slot.js`, and `uploads/` by relative path — keep them side by side. `.nojekyll` stops GitHub Pages from running Jekyll over the files.
