# Escape Velocity (INTRO keynote)

AI Week opening keynote — from faster tasks to a different product development process.

Basis: copied from the `airborne` deck; slides 2–14 carried over as raw material, being reworked against `[INTRO] Slide Outline (final).md` in the escape-velocity project.

Static deck. `index.html` loads `deck-stage.js`, `support.js`, `image-slot.js`, and `uploads/` by relative path — keep them side by side. `.nojekyll` stops GitHub Pages from running Jekyll over the files.
